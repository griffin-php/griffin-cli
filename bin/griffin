#!/usr/bin/env php
<?php

if (php_sapi_name() !== 'cli') {
    exit;
}

require __DIR__ . '/../vendor/autoload.php';

use Griffin\Cli\Command;
use Minicli\App;

$app = new App();

$app->registerCommand('help', new Command\Help());

$app->runCommand($argv);
